<%= content_tag(
  :iframe,
  nil,
  :width => "100%",
  :height => "500px",
  :scrolling => "no",
  :src => guide_scenario_path(
    :scenario_id => scenario_id,
    :node_path => node_path,
    :scenario_format => scenario_format,
  ),
  :data => {:view => ("iframeAutoHeight") }
) %>
