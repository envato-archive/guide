<%= content_tag(  :div,
                  :class => "sg-scenario__iframe is-loading",
                  :data => {
                    :view => "iframe"
                  }
                ) do %>

  <%= content_tag(  :iframe,
                    nil,
                    :width => "100%",
                    :scrolling => "no",
                    :src => "/scenario/#{scenario_id}/#{scenario_format}/for/#{node_path}"
                  ) %>
<% end %>
